# AI Gateway Configuration

# Primary model (leave empty for Copilot's default model)
# Note: --model requires Copilot Business/Enterprise plan
MODEL=""

# Fallback model if primary fails (leave empty for Copilot's default)
FALLBACK_MODEL=""

# Number of retry attempts on failure (per model)
MAX_RETRIES=2

# Request timeout in seconds
TIMEOUT=60
